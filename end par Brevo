warning: in the working copy of 'server.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/server.js b/server.js[m
[1mindex 5ce9e21..9b2e974 100644[m
[1m--- a/server.js[m
[1m+++ b/server.js[m
[36m@@ -66,17 +66,19 @@[m [mif (process.env.TURN_URL) {[m
 // Deux façons d'envoyer le code, choisies automatiquement selon ce qui est[m
 // configuré :[m
 //[m
[31m-// 1) RESEND_API_KEY (recommandé, surtout sur Render) : l'envoi passe par[m
[32m+[m[32m// 1) BREVO_API_KEY (recommandé, surtout sur Render) : l'envoi passe par[m
 //    une requête HTTPS classique (port 443, jamais bloqué par les[m
 //    hébergeurs), au lieu d'une connexion SMTP brute. Render (offre[m
 //    gratuite) bloque les connexions SMTP sortantes vers Gmail (465/587) —[m
 //    la connexion reste bloquée jusqu'au timeout (ETIMEDOUT/ENETUNREACH),[m
[31m-//    quelle que soit la config réseau côté nodemailer. Resend contourne ça[m
[32m+[m[32m//    quelle que soit la config réseau côté nodemailer. Brevo contourne ça[m
 //    entièrement puisque ce n'est qu'un appel HTTP normal.[m
 // 2) EMAIL_USER/EMAIL_PASS (Gmail SMTP, ancien système) : gardé en repli[m
 //    pour un déploiement sur un hébergeur qui n'a pas ce problème de port.[m
[31m-const RESEND_API_KEY = process.env.RESEND_API_KEY || '';[m
[31m-const RESEND_FROM = process.env.RESEND_FROM || 'NullChat <onboarding@resend.dev>';[m
[32m+[m[32m//[m
[32m+[m[32mconst BREVO_API_KEY = process.env.BREVO_API_KEY || '';[m
[32m+[m[32mconst BREVO_FROM_EMAIL = process.env.BREVO_FROM_EMAIL || 'onboarding@nullchat.app';[m
[32m+[m[32mconst BREVO_FROM_NAME = process.env.BREVO_FROM_NAME || 'NullChat';[m
 [m
 const EMAIL_USER = process.env.EMAIL_USER || '';[m
 const EMAIL_PASS = process.env.EMAIL_PASS || '';[m
[36m@@ -96,13 +98,13 @@[m [mif (EMAIL_USER && EMAIL_PASS) {[m
   });[m
 }[m
 [m
[31m-const emailServiceConfigured = !!RESEND_API_KEY || !!mailTransporter;[m
[31m-if (RESEND_API_KEY) {[m
[31m-  console.log(`✅ .env ${dotenvLoaded ? 'chargé' : 'NON chargé (dotenv absent)'} — envoi des codes 2FA via Resend (${RESEND_FROM}).`);[m
[32m+[m[32mconst emailServiceConfigured = !!BREVO_API_KEY || !!mailTransporter;[m
[32m+[m[32mif (BREVO_API_KEY) {[m
[32m+[m[32m  console.log(`✅ .env ${dotenvLoaded ? 'chargé' : 'NON chargé (dotenv absent)'} — envoi des codes 2FA via Brevo (${BREVO_FROM_NAME} <${BREVO_FROM_EMAIL}>).`);[m
 } else if (mailTransporter) {[m
[31m-  console.log(`✅ .env ${dotenvLoaded ? 'chargé' : 'NON chargé (dotenv absent)'} — envoi des codes 2FA via SMTP Gmail (${EMAIL_FROM}). Sur Render, préfère RESEND_API_KEY si le SMTP reste bloqué.`);[m
[32m+[m[32m  console.log(`✅ .env ${dotenvLoaded ? 'chargé' : 'NON chargé (dotenv absent)'} — envoi des codes 2FA via SMTP Gmail (${EMAIL_FROM}). Sur Render, préfère BREVO_API_KEY si le SMTP reste bloqué.`);[m
 } else {[m
[31m-  console.warn('⚠️ Ni RESEND_API_KEY ni EMAIL_USER/EMAIL_PASS définis (.env manquant ?) : la vérification par e-mail (2FA) est désactivée, les connexions se feront sans code.');[m
[32m+[m[32m  console.warn('⚠️ Ni BREVO_API_KEY ni EMAIL_USER/EMAIL_PASS définis (.env manquant ?) : la vérification par e-mail (2FA) est désactivée, les connexions se feront sans code.');[m
 }[m
 [m
 const TWOFA_TTL_MS = 10 * 60 * 1000;     // un code envoyé par mail est valable 10 min[m
[36m@@ -147,30 +149,31 @@[m [mfunction twoFactorEmailHtml(code) {[m
     </div>`;[m
 }[m
 [m
[31m-async function sendViaResend(toEmail, code) {[m
[32m+[m[32masync function sendViaBrevo(toEmail, code) {[m
   // Timeout manuel : fetch n'a pas de timeout par défaut, on ne veut pas[m
   // reproduire le même problème de requête qui pend indéfiniment.[m
   const controller = new AbortController();[m
   const timeout = setTimeout(() => controller.abort(), 10000);[m
   let response;[m
   try {[m
[31m-    response = await fetch('https://api.resend.com/emails', {[m
[32m+[m[32m    response = await fetch('https://api.brevo.com/v3/smtp/email', {[m
       method: 'POST',[m
       headers: {[m
[31m-        'Authorization': `Bearer ${RESEND_API_KEY}`,[m
[31m-        'Content-Type': 'application/json'[m
[32m+[m[32m        'api-key': BREVO_API_KEY,[m
[32m+[m[32m        'Content-Type': 'application/json',[m
[32m+[m[32m        'Accept': 'application/json'[m
       },[m
       body: JSON.stringify({[m
[31m-        from: RESEND_FROM,[m
[31m-        to: [toEmail],[m
[32m+[m[32m        sender: { name: BREVO_FROM_NAME, email: BREVO_FROM_EMAIL },[m
[32m+[m[32m        to: [{ email: toEmail }],[m
         subject: 'Ton code de vérification NullChat',[m
[31m-        text: `Ton code de vérification NullChat est : ${code}\nIl expire dans 10 minutes.\nSi tu n'es pas à l'origine de cette connexion, ignore cet e-mail.`,[m
[31m-        html: twoFactorEmailHtml(code)[m
[32m+[m[32m        textContent: `Ton code de vérification NullChat est : ${code}\nIl expire dans 10 minutes.\nSi tu n'es pas à l'origine de cette connexion, ignore cet e-mail.`,[m
[32m+[m[32m        htmlContent: twoFactorEmailHtml(code)[m
       }),[m
       signal: controller.signal[m
     });[m
   } catch (e) {[m
[31m-    if (e.name === 'AbortError') throw new Error('Resend : délai dépassé (10s).');[m
[32m+[m[32m    if (e.name === 'AbortError') throw new Error('Brevo : délai dépassé (10s).');[m
     throw e;[m
   } finally {[m
     clearTimeout(timeout);[m
[36m@@ -178,10 +181,10 @@[m [masync function sendViaResend(toEmail, code) {[m
 [m
   if (!response.ok) {[m
     const errBody = await response.text().catch(() => '');[m
[31m-    throw new Error(`Resend a refusé l'envoi (HTTP ${response.status}) : ${errBody.slice(0, 300)}`);[m
[32m+[m[32m    throw new Error(`Brevo a refusé l'envoi (HTTP ${response.status}) : ${errBody.slice(0, 300)}`);[m
   }[m
   const data = await response.json().catch(() => ({}));[m
[31m-  log('E-mail 2FA envoyé via Resend, id =', data.id || '(inconnu)');[m
[32m+[m[32m  log('E-mail 2FA envoyé via Brevo, messageId =', data.messageId || '(inconnu)');[m
 }[m
 [m
 async function sendViaGmailSmtp(toEmail, code) {[m
[36m@@ -204,7 +207,7 @@[m [masync function sendViaGmailSmtp(toEmail, code) {[m
 }[m
 [m
 async function sendTwoFactorEmail(toEmail, code) {[m
[31m-  if (RESEND_API_KEY) return sendViaResend(toEmail, code);[m
[32m+[m[32m  if (BREVO_API_KEY) return sendViaBrevo(toEmail, code);[m
   if (mailTransporter) return sendViaGmailSmtp(toEmail, code);[m
   throw new Error('Service mail non configuré.');[m
 }[m
